

/**
 * Logo component renders the logo for the application.
 * 
 * @returns {React.JSX.Element} The JSX code to render the logo.
 */
export function Logo() {
    return (
        <div className="logo">
            <span role="img">🍿</span>
            <h1>usePopcorn</h1>
        </div>
    )
}
